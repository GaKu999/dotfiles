#!/hint/bash
# ${XDG_CONFIG_HOME}/wrapper/i3lock.conf
# vim:ft=bash:fenc=utf-8:noet:ts=3:sw=3:

: ${HOME:?}

cat <<EOF
--color 000000
--ignore-empty-password
--image ${HOME}/.wallpaper
--show-failed-attempt
EOF
